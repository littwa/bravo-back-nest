export enum ERole {
    Customer = 'customer',
    Manager = 'manager',
    Admin = 'admin',
}

export enum EStatus {
    NotVerified = "Not Verified",
    Verified = "Verified",
    NotRequiredVerification = "Not Required Verification"
}